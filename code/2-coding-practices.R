outlier_value <- 100

reaction_time < outliner_value

drt
ov
rt1
df_reaction_time 

drt$rt1 > ov

df_reaction_time$reactiontime_trial1 > outliner_value

Sys.info()
sys.info()

sessionInfo()
sessioninfo()

reaction_time <- 246
reactiontime <- 246

ReactionTime <- 246

reaction.time <- 246

reaction_time <-              248
reaction_time <-       
  
  
  
  
  
  248


print(    "hello"   ) 
print  (  
  
  "hello" 
  
  
) 

mean(1:10, na.rm = TRUE)

### Hard coding --- 
BAD_ID <- 64

id <- 64
# WRONG
if(id == 64) {
  warning("BAD ID")
}

# GOOD 
if(id == BAD_ID) {
  warning("BAD ID")
}

## DRY - 
# DO NOT REPEAT YOURSELF
# NOOPE
if (rt1 < 100) warning("bad reaction time")
if (rt2 < 100) warning("bad reaction time")
if (rt3 < 100) warning("bad reaction time")

outlier_value <- 100
if (rt1 < outlier_value) warning("bad reaction time")
if (rt2 < outlier_value) warning("bad reaction time")
if (rt3 < outlier_value) warning("bad reaction time")

